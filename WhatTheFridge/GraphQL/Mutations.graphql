mutation CreateUser($input: UserInput!) {
  createUser(input: $input) {
    errors {
      field
      message
    }
    user {
      id
      firebaseUserUID
      firstName
      lastName
      email
      imgUrl
      tier
      createdAt
      updatedAt
    }
  }
}
